<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    tools:context="com.group6.babytime.usermanage.Register">
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="请输入手机号（只支持中国大陆手机号）"
        android:textSize="16sp"
        android:id="@+id/phone_number"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="请输入验证码"
        android:textSize="16sp"
        android:id="@+id/et_code"
        android:layout_below="@id/phone_number"
        android:layout_marginTop="10dp"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="获取验证码"
        android:id="@+id/validate_code_btn"
        android:background="#6FD3D5"
        android:textColor="#ffffff"
        android:layout_below="@+id/phone_number"
        android:layout_alignRight="@+id/phone_number"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="8-24位数字，字母，下划线组合"
        android:textSize="16sp"
        android:id="@+id/input_password"
        android:inputType="textPassword"
        android:layout_below="@+id/validate_code_btn"
        android:layout_marginTop="10dp"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="立即注册"
        android:id="@+id/register_btn"
        android:background="#F8CA00"
        android:textColor="#ffffff"
        android:textAlignment="center"
        android:layout_below="@+id/input_password"
        android:layout_marginTop="10dp"
        />
</RelativeLayout>
